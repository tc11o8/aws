<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="com.smalleyes.aws.mapper.SpParamConfigMapper">

	<select id="selectAll" resultType="com.smalleyes.aws.pojo.SpParamConfig">
		SELECT
		item_key itemKey,
		item_value itemValue 
		FROM sp_param_config
	</select>

</mapper>
